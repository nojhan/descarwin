CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(dae_mt NONE)

SET(LATEX_COMPILER_FLAGS "-interaction=nonstopmode --enable-write18" CACHE STRING "Flags passed to latex.")

SET(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake/modules)
INCLUDE(${CMAKE_MODULE_PATH}/UseLATEX.cmake)

SET(SOURCE_INPUTS
  )

ADD_LATEX_DOCUMENT(${PROJECT_NAME}.tex BIBFILES dae_mt.bib INPUTS ${SOURCE_INPUTS} IMAGE_DIRS images DEFAULT_PDF)
